### install ganache software
### `npm install truffle@5.0.2` or `npm install -g truffle@5.0.2` (if permission denied on Macbook, add `sudo` at the beginning)
### `npm install web3`
### install metamask chrome extension